import { AppRoutingModule } from '../app-routing.module';

export class Book {
    key: string;
    title: string;
    description: AppRoutingModule;
    author: string;
    price: number;
    active = true;
  }